# 🌸 Трекер Настроения - Биполярное Расстройство

Полнофункциональное веб-приложение для отслеживания эмоциональных фаз при биполярном расстройстве личности с системой регистрации пользователей и базой данных MongoDB.

## 🎨 Особенности

### Фронтенд
- **Современный дизайн** с нежными розовыми оттенками и плавными анимациями
- **Регистрация и авторизация** с безопасным хранением паролей
- **Трекинг настроения** с тремя типами фаз:
  - 🌧️ Депрессивная фаза
  - 🌤️ Интерфаза (стабильное состояние)
  - ⚡ Маниакальная фаза
- **Шкала интенсивности** от 1 до 10 для каждой записи
- **Личные заметки** к каждой записи
- **Визуализация данных** в виде графика за последние 30 дней
- **Статистика** с подсчетом количества дней в каждой фазе
- **Фильтрация записей** по типу настроения
- **Экспорт данных** в формате JSON для резервного копирования

### Бэкенд
- **Node.js + Express** сервер
- **MongoDB** база данных для хранения пользователей и записей
- **JWT аутентификация** для безопасного доступа
- **RESTful API** для всех операций
- **Валидация данных** на сервере
- **Защита маршрутов** middleware
- **Хеширование паролей** с использованием bcrypt

## 🌐 Публикация в интернет

**Хотите опубликовать сайт в общий доступ?**

👉 Смотрите **[DEPLOY-QUICK.md](DEPLOY-QUICK.md)** - быстрая публикация за 15 минут (БЕСПЛАТНО)

👉 Или **[DEPLOYMENT.md](DEPLOYMENT.md)** - подробное руководство со всеми вариантами

---

## 🚀 Установка и запуск (локально)

### Требования
- Node.js (версия 14 или выше)
- MongoDB (локально или MongoDB Atlas)
- npm или yarn

### Шаг 1: Клонирование и установка зависимостей

```bash
# Установка зависимостей
npm install
```

### Шаг 2: Настройка MongoDB

#### Вариант A: Локальная MongoDB

1. Установите MongoDB Community Edition: https://www.mongodb.com/try/download/community
2. Запустите MongoDB:

```bash
# Windows
net start MongoDB

# macOS/Linux
sudo systemctl start mongod
```

#### Вариант B: MongoDB Atlas (облачная)

1. Создайте бесплатный аккаунт на https://www.mongodb.com/cloud/atlas
2. Создайте кластер
3. Получите строку подключения

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
PORT=3000
MONGODB_URI=mongodb://localhost:27017/bipolar-tracker
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d
```

Для MongoDB Atlas замените `MONGODB_URI` на вашу строку подключения:
```env
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bipolar-tracker
```

### Шаг 4: Запуск приложения

```bash
# Продакшн режим
npm start

# Режим разработки (с автоперезагрузкой)
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

## 📁 Структура проекта

```
bipolarwebsite/
│
├── config/
│   └── db.js                 # Подключение к MongoDB
│
├── middleware/
│   └── auth.js               # Middleware аутентификации
│
├── models/
│   ├── User.js               # Модель пользователя
│   └── Entry.js              # Модель записи настроения
│
├── routes/
│   ├── auth.js               # Маршруты аутентификации
│   └── entries.js            # Маршруты для записей
│
├── public/
│   ├── login.html            # Страница входа/регистрации
│   ├── index.html            # Главная страница приложения
│   ├── style.css             # Стили
│   ├── auth.js               # Логика авторизации
│   └── api-script.js         # Логика приложения с API
│
├── server.js                 # Главный файл сервера
├── package.json              # Зависимости проекта
├── .env                      # Переменные окружения (создать)
├── .gitignore               # Игнорируемые файлы
└── README.md                # Документация
```

## 🔌 API Endpoints

### Аутентификация

#### Регистрация
```
POST /api/auth/register
Content-Type: application/json

{
  "username": "string",
  "email": "string",
  "password": "string"
}
```

#### Вход
```
POST /api/auth/login
Content-Type: application/json

{
  "email": "string",
  "password": "string"
}
```

#### Получить текущего пользователя
```
GET /api/auth/me
Authorization: Bearer {token}
```

### Записи настроения

#### Получить все записи
```
GET /api/entries
Authorization: Bearer {token}

Query Parameters:
- startDate: ISO date (optional)
- endDate: ISO date (optional)
- mood: depressive|interfase|manic (optional)
```

#### Получить статистику
```
GET /api/entries/stats
Authorization: Bearer {token}
```

#### Создать запись
```
POST /api/entries
Authorization: Bearer {token}
Content-Type: application/json

{
  "mood": "depressive|interfase|manic",
  "intensity": 1-10,
  "notes": "string",
  "date": "ISO date"
}
```

#### Обновить запись
```
PUT /api/entries/:id
Authorization: Bearer {token}
Content-Type: application/json

{
  "mood": "depressive|interfase|manic",
  "intensity": 1-10,
  "notes": "string"
}
```

#### Удалить запись
```
DELETE /api/entries/:id
Authorization: Bearer {token}
```

## 🎯 Использование приложения

### Первый запуск

1. Откройте браузер и перейдите на http://localhost:3000
2. Вы увидите страницу входа/регистрации
3. Создайте новый аккаунт на вкладке "Регистрация"
4. После регистрации вы автоматически войдете в систему

### Добавление записей

1. Выберите ваше текущее настроение (депрессивная фаза, интерфаза или маниакальная фаза)
2. Установите интенсивность состояния от 1 до 10
3. Добавьте заметки о вашем состоянии, событиях дня или триггерах
4. Нажмите "Сохранить запись"

### Просмотр данных

- **Статистика** показывает количество дней в каждой фазе
- **График** отображает динамику настроения за последние 30 дней
- **История** содержит все ваши записи с возможностью фильтрации

### Рекомендации

1. **Регулярность**: Заполняйте трекер ежедневно, желательно в одно и то же время
2. **Честность**: Будьте честны с собой о своем состоянии
3. **Детали**: Добавляйте заметки о событиях, триггерах, качестве сна и приеме лекарств
4. **Резервные копии**: Регулярно экспортируйте данные для сохранности
5. **Консультации**: Используйте данные из трекера на встречах с врачом или психотерапевтом

## 🔒 Безопасность

- **Хеширование паролей** с использованием bcrypt
- **JWT токены** для аутентификации с истечением срока действия
- **Защита маршрутов** - доступ только для авторизованных пользователей
- **Валидация данных** на клиенте и сервере
- **Изоляция данных** - каждый пользователь видит только свои записи
- **CORS** настроен для безопасного взаимодействия

## 🛠️ Технические детали

### Технологии

**Backend:**
- Node.js
- Express.js
- MongoDB + Mongoose
- JWT (jsonwebtoken)
- bcryptjs
- express-validator
- CORS

**Frontend:**
- HTML5
- CSS3 (анимации, градиенты, Flexbox, Grid)
- Vanilla JavaScript (ES6+)
- Canvas API для графиков
- Fetch API для запросов

### Анимации
- Плавное появление элементов при загрузке
- Анимированный фон с плавающими элементами
- Интерактивные hover-эффекты
- Пульсирующие кнопки
- Плавные переходы между состояниями

### Адаптивность
- Полностью адаптивный дизайн
- Оптимизация для мобильных устройств
- Поддержка различных размеров экранов

## 🐛 Решение проблем

### Ошибка подключения к MongoDB

```
Error: connect ECONNREFUSED 127.0.0.1:27017
```

**Решение:** Убедитесь, что MongoDB запущена локально:
- Windows: `net start MongoDB`
- macOS/Linux: `sudo systemctl start mongod`

### Ошибка "Пользователь уже существует"

**Решение:** Используйте другой email или имя пользователя для регистрации.

### Токен истек

**Решение:** Выйдите из системы и войдите снова.

### Порт 3000 уже занят

**Решение:** Измените порт в файле `.env`:
```env
PORT=3001
```

## ⚠️ Важное уведомление

**Этот инструмент не заменяет профессиональную медицинскую помощь.**

Если вы испытываете симптомы биполярного расстройства:
- Обратитесь к квалифицированному психиатру или психотерапевту
- Не прекращайте прием назначенных лекарств без консультации с врачом
- В случае суицидальных мыслей немедленно обратитесь за помощью

### Контакты экстренной помощи (Россия)
- Телефон доверия: 8-800-2000-122 (бесплатно, круглосуточно)
- Скорая психиатрическая помощь: 03 или 103

## 🌟 Возможности для развития

- [ ] Импорт данных из JSON
- [ ] Экспорт в PDF для врача
- [ ] Напоминания о заполнении (push-уведомления)
- [ ] Отслеживание лекарств и сна
- [ ] Темная тема
- [ ] Мультиязычность
- [ ] Аналитика и паттерны настроения
- [ ] Мобильное приложение (React Native)
- [ ] Двухфакторная аутентификация
- [ ] Восстановление пароля по email

## 📝 Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Добавление новых зависимостей

```bash
npm install package-name
```

### Структура базы данных

#### Коллекция Users
```javascript
{
  _id: ObjectId,
  username: String,
  email: String,
  password: String (hashed),
  createdAt: Date
}
```

#### Коллекция Entries
```javascript
{
  _id: ObjectId,
  user: ObjectId (ref: User),
  mood: String (enum: depressive, interfase, manic),
  intensity: Number (1-10),
  notes: String,
  date: Date,
  createdAt: Date,
  updatedAt: Date
}
```

## 📄 Лицензия

Этот проект создан для личного использования. Вы можете свободно использовать, модифицировать и распространять его.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте, что MongoDB запущена
2. Проверьте файл `.env`
3. Убедитесь, что все зависимости установлены (`npm install`)
4. Проверьте консоль браузера и терминал сервера на наличие ошибок

---

**Берегите себя и свое ментальное здоровье! 💖**

*Разработано с заботой для людей, живущих с биполярным расстройством.*
