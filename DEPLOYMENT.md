# 🌐 Руководство по публикации сайта в интернет

Это руководство покажет несколько способов опубликовать ваше приложение в общий доступ.

## 🎯 Рекомендуемые варианты

### ⭐ Вариант 1: Render (Рекомендуется - БЕСПЛАТНО)

**Плюсы:** Бесплатный, простой, поддерживает Node.js и MongoDB

#### Шаг 1: Подготовка проекта

1. Создайте файл `.gitignore` (уже есть)
2. Обновите `package.json` для добавления скрипта запуска:

```json
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
```

3. Убедитесь, что используете переменную окружения для порта:
   - ✅ Уже настроено в `server.js`

#### Шаг 2: Создание репозитория на GitHub

```bash
# Инициализируем git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Создаем коммит
git commit -m "Initial commit - Bipolar Mood Tracker"

# Создайте новый репозиторий на GitHub (https://github.com/new)
# Затем подключите его:
git remote add origin https://github.com/ваш-username/bipolar-tracker.git
git branch -M main
git push -u origin main
```

#### Шаг 3: Регистрация на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь (можно через GitHub)
3. Нажмите "New +" → "Web Service"

#### Шаг 4: Подключение репозитория

1. Подключите ваш GitHub аккаунт
2. Выберите репозиторий `bipolar-tracker`
3. Настройте сервис:
   - **Name:** bipolar-tracker
   - **Region:** выберите ближайший (Frankfurt для Европы)
   - **Branch:** main
   - **Runtime:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free

#### Шаг 5: Настройка MongoDB Atlas (облачная база данных)

1. Перейдите на https://www.mongodb.com/cloud/atlas
2. Зарегистрируйтесь (бесплатно)
3. Создайте новый кластер:
   - Выберите **FREE** (M0 Sandbox)
   - Регион: выберите ближайший
   - Cluster Name: bipolar-tracker
4. Настройте доступ:
   - Database Access: создайте пользователя с паролем
   - Network Access: добавьте `0.0.0.0/0` (доступ отовсюду)
5. Получите строку подключения:
   - Нажмите "Connect" → "Connect your application"
   - Скопируйте строку (будет вида: `mongodb+srv://username:password@cluster.mongodb.net/`)

#### Шаг 6: Настройка переменных окружения в Render

В настройках вашего Web Service на Render добавьте:

```
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bipolar-tracker
JWT_SECRET=создайте_сложный_случайный_ключ_минимум_32_символа
JWT_EXPIRE=7d
NODE_ENV=production
```

#### Шаг 7: Деплой

1. Нажмите "Create Web Service"
2. Render автоматически задеплоит приложение
3. Получите URL (будет вида: `https://bipolar-tracker.onrender.com`)

**Готово!** 🎉 Ваш сайт доступен по адресу из шага 7.

---

### ⭐ Вариант 2: Railway (Альтернатива - БЕСПЛАТНО)

**Плюсы:** Очень простой, автоматически определяет настройки

#### Шаг 1: Регистрация

1. Перейдите на https://railway.app
2. Зарегистрируйтесь через GitHub

#### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий

#### Шаг 3: Добавление MongoDB

1. В проекте нажмите "New"
2. Выберите "Database" → "Add MongoDB"
3. Railway автоматически создаст переменную `MONGO_URL`

#### Шаг 4: Настройка переменных

В настройках добавьте:
```
MONGODB_URI=${{MONGO_URL}}
JWT_SECRET=ваш_секретный_ключ_32_символа
JWT_EXPIRE=7d
```

#### Шаг 5: Генерация домена

1. В настройках сервиса перейдите в "Settings"
2. Нажмите "Generate Domain"
3. Получите URL

**Готово!** 🎉

---

### ⭐ Вариант 3: Vercel (Фронтенд) + Render/Railway (Бэкенд)

Если хотите разделить фронтенд и бэкенд:

#### Бэкенд на Render
- Следуйте инструкциям из Варианта 1

#### Фронтенд на Vercel

1. Создайте папку `frontend` и переместите туда файлы из `public/`
2. Зарегистрируйтесь на https://vercel.com
3. Подключите GitHub репозиторий
4. Настройте:
   - **Root Directory:** `frontend`
   - Добавьте переменную окружения:
     ```
     NEXT_PUBLIC_API_URL=https://ваш-бэкенд.onrender.com/api
     ```
5. Обновите `API_URL` в `auth.js` и `api-script.js`:
   ```javascript
   const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api';
   ```

---

### ⚙️ Вариант 4: VPS (DigitalOcean, Linode, AWS)

Для полного контроля:

#### Шаг 1: Создание VPS
1. Зарегистрируйтесь на DigitalOcean/Linode/AWS
2. Создайте Droplet/Instance (Ubuntu 22.04)
3. Подключитесь по SSH

#### Шаг 2: Установка зависимостей
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Установка MongoDB
wget -qO - https://www.mongodb.org/static/pgp/server-6.0.asc | sudo apt-key add -
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
sudo apt update
sudo apt install -y mongodb-org
sudo systemctl start mongod
sudo systemctl enable mongod

# Установка PM2 для запуска приложения
sudo npm install -g pm2
```

#### Шаг 3: Клонирование и запуск
```bash
# Клонирование репозитория
git clone https://github.com/ваш-username/bipolar-tracker.git
cd bipolar-tracker

# Установка зависимостей
npm install

# Создание .env файла
nano .env
# Добавьте ваши переменные окружения

# Запуск с PM2
pm2 start server.js --name bipolar-tracker
pm2 save
pm2 startup
```

#### Шаг 4: Настройка Nginx и SSL

```bash
# Установка Nginx
sudo apt install -y nginx

# Настройка
sudo nano /etc/nginx/sites-available/bipolar-tracker

# Добавьте:
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Активация
sudo ln -s /etc/nginx/sites-available/bipolar-tracker /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# SSL через Let's Encrypt
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.com
```

---

## 📝 Важные изменения для продакшна

### 1. Обновите CORS в `server.js`

```javascript
// Вместо
app.use(cors());

// Используйте
app.use(cors({
    origin: process.env.FRONTEND_URL || 'http://localhost:3000',
    credentials: true
}));
```

### 2. Обновите API URL в фронтенде

В `public/auth.js` и `public/api-script.js`:

```javascript
const API_URL = process.env.API_URL || window.location.origin + '/api';
```

### 3. Добавьте переменную NODE_ENV

В `.env`:
```env
NODE_ENV=production
```

### 4. Обновите `server.js` для продакшна

```javascript
// После инициализации app
if (process.env.NODE_ENV === 'production') {
    // Дополнительные настройки безопасности
    app.use((req, res, next) => {
        res.setHeader('X-Frame-Options', 'DENY');
        res.setHeader('X-Content-Type-Options', 'nosniff');
        res.setHeader('X-XSS-Protection', '1; mode=block');
        next();
    });
}
```

---

## 🔒 Чеклист безопасности для продакшна

- [ ] Измените `JWT_SECRET` на сложный случайный ключ (минимум 32 символа)
- [ ] Используйте HTTPS (SSL сертификат)
- [ ] Настройте CORS для конкретного домена
- [ ] Ограничьте доступ к MongoDB (не 0.0.0.0/0 в MongoDB Atlas)
- [ ] Добавьте rate limiting (защита от DDoS)
- [ ] Настройте мониторинг и логирование
- [ ] Регулярно делайте бэкапы базы данных
- [ ] Используйте переменные окружения для всех секретов

---

## 📊 Мониторинг

### Логи в Render
```
Dashboard → Logs → Real-time logs
```

### Логи в Railway
```
Project → Service → Logs
```

### Логи на VPS
```bash
pm2 logs bipolar-tracker
pm2 monit
```

---

## 🆘 Решение проблем

### "Application failed to start"
- Проверьте логи
- Убедитесь, что все переменные окружения установлены
- Проверьте `package.json` scripts

### "Cannot connect to MongoDB"
- Проверьте `MONGODB_URI` в переменных окружения
- Убедитесь, что IP разрешен в MongoDB Atlas

### "502 Bad Gateway"
- Приложение не запустилось или упало
- Проверьте логи сервера

---

## 💰 Сравнение стоимости

| Платформа | Бесплатно | Цена при масштабировании |
|-----------|-----------|--------------------------|
| **Render** | ✅ 750 часов/месяц | $7/месяц за всегда активный |
| **Railway** | ✅ $5 кредит/месяц | $5-20/месяц |
| **Vercel** | ✅ Безлимит | $20/месяц Pro |
| **DigitalOcean** | ❌ | $6/месяц минимум |
| **AWS/GCP** | 🔶 12 месяцев free tier | $10-100+/месяц |

---

## 🎉 Готово!

После деплоя ваш сайт будет доступен 24/7 в интернете!

**Рекомендация:** Начните с **Render** (вариант 1) - это самый простой и бесплатный способ для начала.

